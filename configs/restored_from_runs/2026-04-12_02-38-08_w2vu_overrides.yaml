# Restored from: outputs/2026-04-12/02-38-08/.hydra/config.yaml
# Purpose: preserve the exact model/task/optimizer settings used for that run.

model:
  smoothness_weight: 1.5
  smoothing: 0
  smoothing_one_sided: false
  gumbel: false
  hard_gumbel: false
  gradient_penalty: 0.5
  code_penalty: 6
  temp: [2, 0.1, 0.99995]
  input_dim: 1024
  segmentation:
    type: JOIN
    mean_pool_join: false
    remove_zeros: false

task:
  data: /home/annaliese/gans_project/wav2vec_unsupervised/data/clustering/librispeech_hf/precompute_pca512_cls128_mean_pooled
  text_data: /home/annaliese/gans_project/wav2vec_unsupervised/data/text/phones/
  kenlm_path: /home/annaliese/gans_project/wav2vec_unsupervised/data/text/phones/lm.phones.filtered.04.bin
  labels: phn
  train_audio_subsample_ratio: 0.5
  subsample_seed: 0

optimizer:
  groups:
    generator:
      optimizer:
        lr: [4.0e-05]
    discriminator:
      optimizer:
        lr: [2.0e-05]
